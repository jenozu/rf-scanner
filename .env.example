# Frontend environment variables (copy to .env for local dev)
# VITE_ prefix required for Vite to expose these to the browser

# API base URL — leave as /api when using the Vite proxy (local dev)
# Set to the full backend URL if running frontend and backend separately
VITE_API_URL=/api
